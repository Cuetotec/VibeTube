<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Variante noche: el tema principal es oscuro de forma fija, así que la
         splash usa el mismo fondo en modo noche para una transición continua. -->
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/vibe_background</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="postSplashScreenTheme">@style/Theme.VibeTube</item>
    </style>
</resources>
